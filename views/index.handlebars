<h1>Burritos!</h1>

<h2>Burritos that are ready to eat!</h2>

<ul>
  {{#each burritos}}
    {{#unless devoured}}
      {{> burritos/burrito-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burritos that have been eaten!</h2>

<ul>
  {{#each burritos}}
    {{#if devoured}}
      {{> burritos/burrito-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burrito</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burrito Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Eat me later!<br>
    <input type="radio" name="devoured" value="0"> Eat me now!
  </div>

  <button type="submit">Add Burrito</button>
</form>